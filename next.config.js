/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.icon-icons.com',
      'upload.wikimedia.org'
    ]
  },
  reactStrictMode: true,
}

module.exports = nextConfig
